# HaemoUpdate

This tool performs the installation of a Haemonetics update package.

## Usage

Provide the path of the (unarchived) update package. Example:

```bash
haemo-update -u /path/to/update/package
```
